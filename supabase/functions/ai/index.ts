// Supabase Edge Function: `ai`
// The ONLY place the browser reaches AI. Holds provider keys (Deno.env),
// selects a provider, and (later) validates output against a JSON schema.
//
// STUB: returns 501 until implemented in the AI phase. No fake completions.
//
// Deno.serve(async (req) => { ... route to getProvider(...).complete(...) ... });
export {};
