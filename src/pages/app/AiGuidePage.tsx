import { PlaceholderPage } from "@/components/common/PlaceholderPage";
export function AiGuidePage() {
  return (
    <PlaceholderPage
      title="AI Guide"
      what="A contextual guide that knows your current product, phase and previous answers."
      why="It gives specific next steps instead of generic chatbot answers — and never re-asks what it already knows."
      next="Wired to the server-side AI boundary once Discovery and product context exist."
    />
  );
}
