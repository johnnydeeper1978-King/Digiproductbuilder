import { PlaceholderPage } from "@/components/common/PlaceholderPage";
export function ContentPage() {
  return (
    <PlaceholderPage
      title="Content"
      what="A workspace for the marketing content tied to your product and launch."
      why="Content is how the product gets sold — kept in one place per product."
      next="Built alongside the Builder's marketing and launch phases."
    />
  );
}
