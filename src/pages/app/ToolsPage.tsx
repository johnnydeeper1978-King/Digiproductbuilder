import { PlaceholderPage } from "@/components/common/PlaceholderPage";
export function ToolsPage() {
  return (
    <PlaceholderPage
      title="Tools"
      what="Contextual, recommended tools for whatever stage you're on."
      why="The right tool at the right step beats one generic tools page."
      next="Recommendations (and any affiliate links) are surfaced contextually inside the Builder phases."
    />
  );
}
